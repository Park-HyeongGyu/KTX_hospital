# KTXHospital Handoff Notes (2026-04-23)

이 문서는 2026-04-23 기준으로 이 저장소에서 진행된 작업, 코드 상태, 산출물, 해석, 문제의식, 남은 연구 과제를 최대한 자세히 정리한 handoff 문서다. 다른 AI가 이 문서 하나만 읽고도 현재 상황을 최대한 빠르게 파악할 수 있도록 작성했다.

## 1. 프로젝트 목표와 현재 분석 맥락

현재 분석의 큰 주제는 KTX 접근성 변화가 지역 의료공급에 어떤 영향을 주었는지를 지역-연도 패널 수준에서 보는 것이다.  
최근 작업은 주로 다음 순서로 진행되었다.

1. `clean_hospital_by_type.parquet`와 `clean_shock_30.parquet`를 합쳐서 분석용 `base` 패널 생성
2. `base`를 바탕으로 병원종별 분석용 패널 생성
3. summary statistics 생성 및 LaTeX 표 저장
4. TWFE DiD 회귀 실행 및 LaTeX 표 저장
5. 30km와 20km 결과를 한 개의 LaTeX view 문서에 정리
6. 결과가 이상해 보여 treatment 정의 및 연구설계 자체를 재검토하는 단계로 넘어감

현재 핵심 문제의식은 다음과 같다.

- centroid 기반 거리 기준(`10/20/30/40/50km`)으로 KTX shock을 정의하는 것이 실제 접근성 개선을 제대로 잡아내지 못하고 있을 가능성이 크다.
- 예천처럼 실제 이용은 버스가 더 중요할 수 있고, 광주처럼 “KTX 접근성이 원래 높거나 개선으로 보기 어려운 대도시권”도 treated로 잡힐 수 있다.
- 따라서 지금부터는 단순 코딩보다는 treatment definition과 identification strategy를 어떻게 설계할지의 문제가 핵심이라는 판단까지 와 있다.

## 2. 저장소 구조와 관련 파일

이번 작업과 직접 관련된 핵심 파일은 다음과 같다.

- 분석 스크립트: `src/analysis_30_2026-0423.r`
- 환경설정: `config.r`
- clean hospital 생성 스크립트: `src/clean_hospital.r`
- 현재 view 문서: `temp/view_2026-0423.tex`
- 결과 폴더:
  - `output/30km`
  - `output/20km`
- 정리용 쉘 스크립트: `clean.sh`

현재 `output` 폴더에는 아래 파일들이 있다.

### `output/30km`

- `summary_stats_full.tex`
- `summary_stats_clinic.tex`
- `summary_stats_secondary.tex`
- `summary_stats_general.tex`
- `summary_stats_tertiary.tex`
- `twfe_did_full.tex`
- `twfe_did_clinic.tex`
- `twfe_did_secondary.tex`
- `twfe_did_general.tex`
- `twfe_did_tertiary.tex`

### `output/20km`

- `summary_stats_full.tex`
- `summary_stats_clinic.tex`
- `summary_stats_secondary.tex`
- `summary_stats_general.tex`
- `summary_stats_tertiary.tex`
- `twfe_did_full.tex`
- `twfe_did_clinic.tex`
- `twfe_did_secondary.tex`
- `twfe_did_general.tex`
- `twfe_did_tertiary.tex`

## 3. 현재 워크트리 상태

`git status --short` 기준으로 현재 워크트리는 다음과 같다.

- 수정됨:
  - `clean_parquet/clean_shock_10.parquet`
  - `clean_parquet/clean_shock_20.parquet`
  - `clean_parquet/clean_shock_30.parquet`
  - `clean_parquet/clean_shock_40.parquet`
  - `clean_parquet/clean_shock_50.parquet`
  - `src/clean_map.r`
- 새 파일:
  - `clean.sh`
  - `clean_csv/merge_key.csv`
  - `output/`
  - `raw_data/manual/ktx_shock_date_removed.xlsx`

이 의미는 중요하다.

- `clean_shock_*.parquet`는 사용자 쪽에서 이미 수정된 상태다.
- 따라서 현재 결과물은 원래 shock 정의가 아니라 “사용자가 일부 역/지역을 손본 이후의 shock 정의”를 반영하고 있을 가능성이 높다.
- 특히 사용자 대화상 “김제하고 장성 빼고 돌려봤다”고 했으므로, 현재 `clean_shock_20.parquet`, `clean_shock_30.parquet` 등은 초기에 assistant가 봤던 shock 정의와 다를 수 있다.
- 이 수정은 `analysis_30_2026-0423.r` 내부 로직이 아니라 입력 데이터 자체의 변경이다.

다른 AI가 이어서 작업할 때는 “코드 변화”와 “입력 shock 파일 변화”를 반드시 분리해서 이해해야 한다.

## 4. 초기 분석 스크립트 작업 내역

### 4.1 base 생성

`src/analysis_30_2026-0423.r`에서 현재 `base`는 다음 규칙으로 생성된다.

- 병원 데이터: `clean_parquet/clean_hospital_by_type.parquet`
- shock 데이터: `clean_parquet/clean_shock_30.parquet`
- merge key: `year`, `region_sido`, `region_sigungu`

추가 필터:

1. `region_sido`가 `서울`, `경기`, `인천`, `제주`이면 제외
2. `shock_year <= 2007`이면 already treated로 간주하고 해당 지역 전체 제외
3. 최종 분석 연도는 `year >= 2008`

즉 현재 스크립트는 다음과 같은 해석이다.

- “2008년 이전에 이미 treatment를 받은 지역”은 분석 표본에서 통째로 뺀다.
- 이는 원래 사용자가 “2007년이나 그 이전 already treated를 다 빼고 싶다”고 명시해서 그렇게 구현된 것이다.

### 4.2 `analysis_db.duckdb` 이슈

처음에는 `analysis_db.duckdb`가 0바이트여서 invalid DuckDB file 에러가 있었다.  
이후 사용자가 DB 파일을 정상화했고, 지금은 유효한 DB 파일이다.

현재 확인 결과 `analysis_db.duckdb`에는 적어도 `base`, `panel_full`, `panel_clinic`, `panel_secondary`, `panel_general`, `panel_tertiary`가 존재한다.  
반면 `summary_stats_*`는 생성 후 다시 drop 하도록 했기 때문에 DB에 남지 않는다.

## 5. panel 테이블 정의

사용자 요구로 `table_*` 대신 `panel_*` 이름을 쓰기로 정리했다.

현재 생성되는 DuckDB 테이블은 아래와 같다.

- `panel_full`
- `panel_clinic`
- `panel_secondary`
- `panel_general`
- `panel_tertiary`

정의:

- `panel_full`: `base` 전체
- `panel_clinic`: 해당 지역에서 `의원`이 한 번이라도 `hospital_no >= 1`이었던 적이 있으면 그 지역의 전체 연도 패널 포함
- `panel_secondary`: `병원` 기준 동일
- `panel_general`: `종합병원` 기준 동일
- `panel_tertiary`: `상급종합병원` 기준 동일

중요:

- 이 패널은 “매년 그 병원종별이 존재하는 지역”이 아니라, “역사상 한 번이라도 존재한 적 있는 지역” 전체 연도 패널이다.
- 따라서 어떤 연도에는 해당 hospital type의 `hospital_no = 0`인 region-year가 포함될 수 있다.
- 이 점이 나중에 rate 변수 observation 수 차이의 핵심 원인이 된다.

## 6. summary statistics 블록의 설계와 변화

### 6.1 구현 방식

summary statistics는 `base`를 직접 쓰지 않고 `panel_*`을 입력으로 쓰는 구조로 바뀌었다.

흐름:

1. DuckDB에서 `panel_*`별로 `summary_stats_*` 테이블 생성
2. R에서 `dbReadTable()`로 읽어서 LaTeX 테이블 저장
3. `summary_stats_*` 테이블 drop
4. 블록 전용 함수 제거 + `gc()`

생성되는 결과 테이블 이름:

- `summary_stats_full`
- `summary_stats_clinic`
- `summary_stats_secondary`
- `summary_stats_general`
- `summary_stats_tertiary`

저장 파일:

- `summary_stats_full.tex`
- `summary_stats_clinic.tex`
- `summary_stats_secondary.tex`
- `summary_stats_general.tex`
- `summary_stats_tertiary.tex`

### 6.2 출력 포맷 관련 변화

summary statistics 표 생성 과정에서 몇 가지 포맷 관련 변경이 있었다.

1. 처음에는 `tinytable`, `modelsummary` 등도 검토했지만 결국 단순 데이터프레임 표에는 `knitr::kable()`이 더 적절하다고 판단
2. `.tex`는 `\begin{table}` wrapper 없이 `\begin{tabular}`만 나오도록 유지
3. 지역명(`regions`)은 너무 길기 때문에 아래 규칙 적용
   - 최대 15개까지만 표기
   - 그 뒤는 `...`
   - 4개씩 한 줄에 배치
   - LaTeX에서는 `\shortstack[l]{...}` 사용
4. 사용자가 지역 구분이 어렵다고 해서 각 row 사이에 `\midrule` 추가
5. 처음 헤더가 `shock_year`라서 LaTeX underscore 에러가 났고, 이를 `shock year`로 변경

현재 summary 표는 row마다 얇은 줄이 있고, 지역 리스트는 4개씩 줄바꿈되며, 최대 15개만 표시된다.

## 7. TWFE DiD 블록의 설계와 변화

### 7.1 기본 회귀식

사용자가 요청한 기본 식은:

`Y = beta * ktx_shock_did + region FE + year FE + error`

clustered SE는 region 단위.

구현은 `fixest::feols()`와 `fixest::etable()`을 이용했다.

회귀식:

- `outcome ~ ktx_shock_did | region + year`
- `cluster = ~region`

outcomes:

- `hospital_no`
- `net_entry`
- `exit_no`
- `exit_rate`
- `entry_no`
- `entry_rate`

### 7.2 panel별 처리 방식

`full`과 나머지 종별 패널 처리 방식이 다르다.

#### `full`

`panel_full`은 hospital type별 행이 여러 개 있으므로, 회귀 전에 DuckDB에서 region-year 단위로 합산했다.

- `SUM(hospital_no)`
- `SUM(net_entry)`
- `SUM(exit_no)`
- `SUM(entry_no)`
- `exit_rate = SUM(exit_no) / SUM(hospital_no)` if denominator > 0 else `NULL`
- `entry_rate = SUM(entry_no) / SUM(hospital_no)` if denominator > 0 else `NULL`
- `ktx_shock_did = MAX(ktx_shock_did)`

#### `clinic`, `secondary`, `general`, `tertiary`

각각 해당 `hospital_type`만 남겨서 회귀했다.

- `clinic`: `의원`
- `secondary`: `병원`
- `general`: `종합병원`
- `tertiary`: `상급종합병원`

### 7.3 표 출력 포맷

사용자 요청:

- Stata `esttab` 비슷한 느낌
- coefficient 아래 괄호 안에 SE
- 별표 표시
- `R^2`도 표에 포함
- region FE, year FE는 각각 별도 줄

현재는 `fixest::etable()` 원본을 그대로 쓰는 구조다.

- coefficient row + SE row
- significance stars
- `Observations`
- `R^2`
- `region fixed effects`
- `year fixed effects`

사용자는 한때 `time fixed effects`라고 바꾸길 원했지만, 이후 “후처리하지 말고 fixest가 붙여주는 대로 가자”고 했기 때문에 지금은 `year fixed effects` 그대로 유지된다.

### 7.4 상수 종속변수 처리

처음에는 `panel_tertiary`에서 `exit_no`, `exit_rate`가 상수라 `feols()`가 에러를 내고 스크립트가 중단되었다.

이를 해결하기 위해 현재는 다음 규칙을 적용한다.

- outcome이 전부 `NA`이거나
- non-missing unique value가 1개 이하이면
- 그 outcome은 자동으로 skip

그리고 `.tex` 상단에 주석으로

`% Skipped outcomes (constant or all NA): ...`

를 붙인다.

예:

- `twfe_did_tertiary.tex`에서는 `exit_no`, `exit_rate`가 skip 된다.

## 8. observation 수가 count와 rate에서 다른 이유

이건 사용자가 강하게 물어봤던 포인트라 별도로 정리한다.

### 8.1 원인

현재 원자료 생성 단계에서 rate 변수는 다음과 같이 정의된다.

- `hospital_no = 0`이면 `exit_rate = NULL`
- `hospital_no = 0`이면 `entry_rate = NULL`

즉 분모가 0이면 rate를 0으로 두지 않고 결측으로 둔다.

그 결과:

- `hospital_no`, `net_entry`, `exit_no`, `entry_no`는 해당 관측치를 유지
- `exit_rate`, `entry_rate`는 같은 관측치를 회귀에서 drop

### 8.2 왜 이런 행이 생기나

`panel_secondary`, `panel_general`, `panel_tertiary`는 “해당 타입 병원이 역사상 한 번이라도 존재했던 지역”의 전체 연도 패널을 포함한다.  
따라서 어떤 지역은 한 연도에는 해당 병원종별이 존재했지만, 다른 연도에는 0개일 수 있다.

예:

- 2015년에는 종합병원이 있었음
- 2010년에는 종합병원이 0개

이런 연도는 `hospital_no = 0`, 따라서 rate는 `NA`.

### 8.3 실제 예시

직접 확인한 숫자는 다음과 같다.

- 30km `panel_full` region-year 집계 후 `SUM(hospital_no)=0`인 행: 18개
  - 따라서 `hospital_no` observation 1,980 vs `rate` observation 1,962
- 30km `panel_secondary`에서 `hospital_no = 0`인 행: 121개
- 30km `panel_general`에서 `hospital_no = 0`인 행: 113개
- 30km `panel_tertiary`에서 `hospital_no = 0`인 행: 53개

이 설명은 `temp/view_2026-0423.tex` 문서 맨 위 Note에도 한국어로 추가되어 있다.

## 9. 광주가 빠졌던 문제와 현재 이해

한 시점에 사용자가 “모든 지역이 다 포함된 게 아닌 것 같다, 광주는 왜 없냐”고 물었다.

확인 결과:

- `clean_hospital_by_type.parquet`에는 광주 행이 존재
- `clean_shock_30.parquet`에도 광주 행이 존재
- 하지만 광주 5개 구 모두 `shock_year = 2004`로 잡혀 있었음
- source row 기준으로는 `장성역`, `호남선`, `ktx_date = 2004-04-01`

즉 당시 광주가 빠진 이유는 merge 실패가 아니라:

- `shock_year <= 2007`인 already treated 지역 전체를 제외하는 현재 규칙

때문이었다.

다만 이후 사용자가 `김제`, `장성`을 제외하고 다시 돌렸다고 했고, 실제 최신 `output/20km`, `output/30km`의 summary table을 보면 광주 5개 구가 2015 treated로 들어와 있다.  
즉 현재 결과물은 “광주가 완전히 빠진 초기 상태”가 아니라, 이미 사용자가 일부 shock definition을 수정한 이후의 결과에 해당한다.

이 차이는 반드시 유념해야 한다.

## 10. 30km와 20km 최신 결과 요약

현재 `output/30km`, `output/20km`는 사용자가 “김제와 장성을 빼고” 다시 생성했다고 밝힌 최신 결과물이다.

### 10.1 30km TWFE 요약

#### full

- `hospital_no`: `3.867**`
- `net_entry`: `0.0905`
- `exit_no`: `-0.2895*`
- `exit_rate`: `-0.0020`
- `entry_no`: `-0.1990`
- `entry_rate`: `-0.0012`

#### clinic

- `hospital_no`: `3.288*`
- `net_entry`: `0.3129`
- `exit_no`: `-0.4121***`
- `exit_rate`: `-0.0031`
- `entry_no`: `-0.0992`
- `entry_rate`: `-0.0010`

#### secondary

- `hospital_no`: `0.4414`
- `net_entry`: `-0.1837*`
- `exit_no`: `0.1377*`
- `exit_rate`: `0.0209`
- `entry_no`: `-0.0459`
- `entry_rate`: `-0.0015`

#### general

- 대체로 전부 유의하지 않음

#### tertiary

- `exit_no`, `exit_rate` skip
- 나머지도 유의하지 않음

### 10.2 20km TWFE 요약

#### full

- `hospital_no`: `10.26***`
- `net_entry`: `0.2525`
- `exit_no`: `-0.5021**`
- `exit_rate`: `0.0009`
- `entry_no`: `-0.2496`
- `entry_rate`: `0.0059`

#### clinic

- `hospital_no`: `9.082***`
- `net_entry`: `0.6459**`
- `exit_no`: `-0.6856***`
- `exit_rate`: `-0.0013`
- `entry_no`: `-0.0396`
- `entry_rate`: `0.0056`

#### secondary

- `hospital_no`: `1.002**`
- `net_entry`: `-0.3330**`
- `exit_no`: `0.1975**`
- `exit_rate`: `0.0215`
- `entry_no`: `-0.1355`
- `entry_rate`: `-0.0117`

#### general

- 대체로 유의하지 않음

#### tertiary

- `exit_no`, `exit_rate` skip
- 나머지도 유의하지 않음

### 10.3 결과 해석에 대한 현재 판단

사용자는 결과가 “뭔가 이상하다”고 느꼈고, assistant도 그 판단에 대체로 동의했다.

핵심 이유:

1. `20km`가 `30km`보다 효과가 훨씬 크게 나온다.
   - 예: `full hospital_no`, `clinic hospital_no`
2. `full`과 `clinic` 결과가 거의 같은 방향으로 매우 크게 나타난다.
   - 사실상 전체 결과가 의원 결과에 끌려간다는 의미일 수 있음
3. `general`, `tertiary`는 거의 반응이 없음
4. `secondary`는 방향이 일관적이지 않고 해석이 애매함
   - `hospital_no`는 양(+)
   - `net_entry`는 음(-)
   - `exit_no`는 양(+)

이 때문에 현재 결과는 “KTX가 의료공급을 명확하게 늘렸다”는 증거로 보기보다,

- 어떤 지역을 treated로 넣었는가
- treatment definition을 어떻게 바꿨는가

에 따라 결과가 크게 흔들리는 신호로 보는 게 더 타당하다고 평가했다.

특히 광주처럼 큰 도시권이 treated로 포함되면 의원 수(`clinic`, `full hospital_no`)가 크게 움직일 수 있는데, 이것이 KTX 때문인지 도시 성장 때문인지 분리하기 어렵다.

현재 상대적으로 plausibility가 있는 메시지는:

- “treated 지역에서 의원 퇴출이 줄어드는 경향이 있을 수 있다”

정도이며, 대규모 `hospital_no` 증가를 곧바로 causal effect로 읽는 것은 위험하다고 봤다.

## 11. summary statistics에서 treatment 구성 변화

최신 summary table을 보면 20km와 30km의 treated composition이 많이 다르다.

예를 들어 `summary_stats_full.tex` 기준:

### 30km

- 2015 treated에 광주 5개 구와 여러 전남 지역이 포함
- 2021 treated에 예천 포함

### 20km

- 2015 treated에도 광주 5개 구가 포함
- 2024 treated라는 새로운 그룹이 존재
  - `경남 양산시`
  - `부산 기장군`
- `NA` 그룹이 훨씬 큼

즉 cutoff가 바뀌면 effect 크기뿐 아니라 treatment timing composition 자체가 달라진다.  
이 역시 현재 전략의 취약성을 보여준다.

## 12. 사용자가 제기한 실질적 연구 문제

사용자 메모 요지:

- 예천은 지도상 KTX shock을 받은 것처럼 보이지만 실제로는 버스 이용이 더 중요하다고 함
- 장성역 때문에 광주가 treatment에 잡히는 것이 직관적으로 이상함
- centroid 기반 분류가 “진짜 KTX 접근성 개선”을 제대로 포착하지 못하는 것 같음

assistant가 이에 대해 정리한 핵심 논점:

1. 현재 문제는 코딩보다 treatment definition 문제
2. `distance-to-station`은 “proximity”일 뿐이고 “true accessibility improvement”가 아님
3. travel time improvement가 더 적절한 treatment일 가능성이 큼

## 13. 연구 설계 측면에서 논의된 다음 단계 아이디어

### 13.1 사용자가 직접 제안한 아이디어

1. travel time으로 엄밀하게 KTX shock 정의하기
2. population 등으로 대도시/중소도시 나눠서 보기

assistant는 둘 다 매우 좋은 방향이라고 평가했다.

### 13.2 추가로 제안된 아이디어

1. `travel time reduction`을 continuous treatment 또는 threshold-based treatment로 사용
2. binary shock 대신 intensity treatment
   - 시간절감분
   - 접근시간 감소율
   - 운행빈도 증가분
3. supply shock와 usable access shock를 분리
4. event study로 pretrend 확인
5. placebo / falsification
   - 가짜 shock year
   - 먼 지역 placebo
6. treated region hand-check
   - shock year별 지역 리스트를 사람이 직접 검토
7. population, density, baseline hospital stock에 따른 heterogeneity
8. staggered adoption 문제를 고려한 Callaway & Sant’Anna류 접근 검토

assistant가 제안한 우선순위:

1. travel time 기반 treatment 정의
2. treated 지역 hand-check
3. event study
4. 도시 규모별 heterogeneity
5. placebo
6. 필요시 TWFE 대신 더 적절한 staggered DiD

## 14. view 문서 관련 작업

`temp/view_2026-0423.tex`는 원래 아주 최소 템플릿이었지만, 현재는 아래 구성을 가진 문서로 확장되었다.

### 문서 구조

- `Note`
  - count와 rate observation 수 차이 설명 (한국어)
- `30km`
  - Summary Statistics 5개
  - TWFE DiD 5개
- `20km`
  - Summary Statistics 5개
  - TWFE DiD 5개

현재 출력 PDF:

- `temp/view_2026-0423.pdf`

최근 컴파일은 성공했으며 9페이지 문서가 생성된다.  
아주 작은 `Overfull \hbox` 경고는 있으나 fatal error는 없다.

## 15. `clean.sh` 스크립트

사용자 요청으로 루트에 `clean.sh`를 만들었다.

기능:

- `output`
- `clean_csv`
- `clean_parquet`

에서 `.gitkeep`만 남기고 나머지를 모두 삭제한다.

즉 “소스코드는 유지한 채, 산출물과 정제 데이터만 비우는” 용도다.

경로:

- `clean.sh`

동작:

```bash
bash clean.sh
```

## 16. 현재 `src/analysis_30_2026-0423.r`의 중요한 특징

다른 AI가 코드를 수정할 때 특히 알아야 할 점:

1. `OUTPUT`은 하드코딩처럼 `output/30km`로 설정되어 있다.
2. 입력 shock 파일은 `clean_parquet/clean_shock_30.parquet`이다.
3. `panel_*`은 DB에 남고, `summary_stats_*`는 drop된다.
4. `TWFE` 블록은 rate에서 `NULL`이 있으면 observation 수가 줄어드는 구조를 그대로 유지한다.
5. `etable()` LaTeX는 후처리 없이 fixest 원본을 저장한다.
6. summary statistics는 `kable()`로 직접 만든다.
7. `did` 패키지는 로드되어 있지만 현재 스크립트에서 실제 사용되지는 않는다.

## 17. 아직 정리되지 않은 잠재적 문제들

### 17.1 콘솔 출력이 지저분함

`dbExecute()` 반환값이 그대로 출력되어 콘솔에 `[1] 7200` 같은 숫자가 많이 보인다.  
분석 결과에는 영향 없지만 읽기 불편하다.

### 17.2 20km 스크립트 위치 불명확

현재 `output/20km` 결과는 존재하지만, 이번 세션에서는 20km용 source script를 직접 수정하지 않았다.  
따라서 20km 산출물이 어떤 소스에서 생성되었는지 별도로 확인이 필요할 수 있다.

### 17.3 input data 변경 이력 불명확

`clean_shock_*.parquet`와 `src/clean_map.r`가 수정된 상태이고 `raw_data/manual/ktx_shock_date_removed.xlsx`가 추가되어 있다.  
이 변경이 어떤 원칙으로 이루어졌는지 완전히 문서화되어 있지 않다.  
즉, 현재 결과는 source code만 읽어서는 완전히 재현되지 않을 수 있다.

### 17.4 연구 설계상 treatment misclassification 우려

현재 가장 큰 substantive issue.

- 예천: centroid상 treated일 수 있으나 실제 이용은 버스 중심
- 광주: 장성역/호남선 등으로 접근성이 잡히지만 “진짜 KTX shock”인지 애매
- 따라서 current cutoff strategy의 external validity / construct validity가 약하다

## 18. 다른 AI가 이어서 할 일 추천

다음 AI가 바로 이어서 하면 좋은 작업은 아래와 같다.

### 높은 우선순위

1. 현재 `clean_shock_20.parquet`, `clean_shock_30.parquet`가 어떤 기준으로 수정되었는지 확인
2. 20km 결과를 만든 source script 위치 확인
3. treated region list를 shock year별로 CSV/markdown으로 정리하여 manual check하기
4. travel time 기반 treatment 정의 초안 설계

### 중간 우선순위

1. event study 코드 작성
2. population / baseline hospital stock 기준 heterogeneity 설계
3. placebo specification 제안

### 낮은 우선순위

1. 콘솔 출력 정리
2. summary/TWFE 생성 로직을 함수화하여 20km/30km 공용 파이프라인으로 정리

## 19. 요약 한 줄

현재 프로젝트는 “30km/20km 기준으로 summary statistics와 TWFE DiD 표를 일단 만들어 본 상태”까지 왔고, 결과를 읽어본 뒤 “이제 진짜 중요한 문제는 centroid 기반 distance cutoff가 아닌, 실제 접근성 개선을 반영하는 treatment definition과 연구 설계”라는 판단으로 넘어간 단계다.
